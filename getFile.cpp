#include <iostream>
#include <fstream>

int main(){
    ofstream.Android;
  
  Android.open ("data" && "com.whatsapp" ios::app){
    if(!Android.fail()){
      std::<h1>Error!</h1>
        } else {
      Android.downloads() || Android.open(){
        downloads"cd Downloads" [file.zip] @get.downloads_Android.downloads()
          }
      
      system("cls");
      syatem("open");
      
      std::print << endl;
      
      retrun 0;
}
